{
	"name": "@agi-cli/cli",
	"version": "0.1.135",
	"description": "AI-powered development assistant CLI",
	"author": "ntishxyz",
	"license": "MIT",
	"homepage": "https://github.com/ntishxyz/agi#readme",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ntishxyz/agi.git",
		"directory": "apps/cli"
	},
	"bugs": {
		"url": "https://github.com/ntishxyz/agi/issues"
	},
	"keywords": [
		"ai",
		"cli",
		"development",
		"assistant",
		"code-review",
		"anthropic",
		"openai",
		"gemini"
	],
	"type": "module",
	"bin": {
		"agi": "./dist/agi"
	},
	"files": [
		"dist/agi*",
		"README.md",
		"LICENSE"
	],
	"scripts": {
		"dev": "bun run index.ts",
		"prebuild": "bun run ../../scripts/build-web.ts",
		"build": "bun run prebuild && bun build --compile --minify ./index.ts --outfile dist/agi",
		"build:darwin-arm64": "bun run prebuild && mkdir -p dist && bun build --compile --minify --target=bun-darwin-arm64 ./index.ts --outfile dist/agi-darwin-arm64",
		"build:darwin-x64": "bun run prebuild && mkdir -p dist && bun build --compile --minify --target=bun-darwin-x64 ./index.ts --outfile dist/agi-darwin-x64",
		"build:linux-x64": "bun run prebuild && mkdir -p dist && bun build --compile --minify --target=bun-linux-x64 ./index.ts --outfile dist/agi-linux-x64",
		"build:linux-arm64": "bun run prebuild && mkdir -p dist && bun build --compile --minify --target=bun-linux-arm64 ./index.ts --outfile dist/agi-linux-arm64",
		"prepublishOnly": "bun run build",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@agi-cli/database": "workspace:*",
		"@agi-cli/sdk": "workspace:*",
		"@agi-cli/server": "workspace:*",
		"@clack/prompts": "^0.11.0",
		"@solana/spl-token": "^0.4.14",
		"chalk": "^5.6.2",
		"commander": "^14.0.2",
		"diff": "^8.0.2",
		"marked": "^16.3.0",
		"marked-terminal": "^7.3.0",
		"qrcode-terminal": "^0.12.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/marked-terminal": "^6.1.1",
		"typescript": "^5"
	}
}
